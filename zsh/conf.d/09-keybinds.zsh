# ------------------------------------------------------------------------------
# Key Bindings
# ------------------------------------------------------------------------------
# Note: This file is loaded AFTER completion setup to avoid conflicts

# Ctrl+O で AWS プロファイル選択を起動（--profile の後で使用）
bindkey '^O' _aws_profile_fzf
