# ------------------------------------------------------------------------------
# Key Bindings
# ------------------------------------------------------------------------------
# Note: This file is loaded AFTER completion setup to avoid conflicts

# Ctrl+P で AWS プロファイル選択を起動（--profile の後で使用）
bindkey '^P' _aws_profile_fzf
